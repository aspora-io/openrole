FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies with force to handle workspace issues
RUN npm install --force

# Copy all app files
COPY . .

# Build the application
RUN npm run build

EXPOSE 3000

# Set production environment
ENV NODE_ENV=production

# Start in production mode
CMD ["npm", "run", "start"]